"use client";
import React, { useState } from "react";
import { useStore } from "@context/store";


function page() {
  const { currentAdmin } = useStore();
  return (
    <div className="">
     
    </div>
  );
}

export default page;
